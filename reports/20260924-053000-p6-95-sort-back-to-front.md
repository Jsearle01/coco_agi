## Form B Report — T-P0-149 / P6.95 — Sort back to front, then stop stamping priority
**Class:** integration (§4A). wip.

### 0 — Receipt / status (C-35 stamp)
t0=2026-09-24 (HEAD `f7e7a06`, wip). git status clean at t0.

### 1 — Summary

**The sort landed. The priority write stays, and §1.1(2) is refuted twice over.**

★★★★★ **AGI sorts, and the key is not priority.** `sortOrder` is **`yPos`**, or
**`priorityToY(priority)` when `fFixedPriority` is set** [`sprite.cpp:97-103`], ascending, with ties
broken by the object's **original** order — a stable `Common::sort` [`sprite.cpp:41-48, 63, 80`].
★★★★ **We appended in object-number order, and that was a divergence from the oracle, not a choice.**

★★★★★ **The write cannot go, and the fidelity argument kills it before the reader question does:**
**`putPixel(curX, curY, GFX_SCREEN_MASK_ALL, curColor, viewPriority)`** [`sprite.cpp:350`], and
`MASK_ALL` includes PRIORITY. ★★★★ **The oracle stamps sprite priority for every drawn pixel exactly
as we do** — removing ours would be a divergence, not an optimisation of an operation "the loop
should not need to do". ★★★ **And it has a live reader**: `co_checkctrl` walks *down* the priority
column, and a tall later sprite's upper rows sit above an earlier sprite's bottom rows.

> ★★★★★ **§6's second trigger: deliver the sort, keep the write.** ★★★ So this task is a **fidelity**
> task, and the speed figures are unchanged by design: **0.2003 standing / 0.2336 moving**, to the
> digit.

★★★★★ **The sort changed real draw order.** View 97 — the flag at `y=17`, priority 15 — carries
`fFixedPriority`, so its key is `priorityToY(15) = 168` and it moves **from index 1 to last**. Staged
keys are now **100, 135, 161, 168**, ascending.

★★★★★ **And the fault arm COULD NOT FIRE in the castle** — reversing the sort produced **identical
planes**, because the castle's sprites never overlap (99 of 99 isolated). It goes red only under
`-ForceOverlap`. ★★★★ **That is why the unsorted order survived undetected: `comp` is 124/124 and
our corpus cannot observe draw order at all.**

**All gates green. All nine arms moved** (+176/+178 B, unconditional) and were **re-baselined**.
**Region A is down to 151 bytes** from 223. Jay: *"1.yes 2.same 3.no"* — layering correct, no speed
change, nothing wrong.

### 2 — Files modified
- `src/harness/p3b_probe.s` — `P3_SPR_SIZE` 6→7 with `sortOrder`; the key computed in `pss_lp`;
  `pss_sort` (insertion, stable); `pca_lp`'s `leay` over the new byte; `-DP3B_SORT_REVERSE`; §9
  deltas 1 and 3.
- `src/harness/composite.s` — §9 delta 2: what the stamp is for and why it stays (comment only).
- `harness/tools/p3b_run.lua` — `SPR_SIZE` constant replacing four hardcoded `* 6` strides.
- `harness/tools/p3b_show.ps1` — `-SortReverse`.
- `harness/tools/p3b_arms_check.ps1` — all nine arms re-baselined, with the retired figures kept.

### 3 — Reasoning

**§3(1)** — the nine arms were at P6.93's figures before this task and all nine moved; see AC-10.

**§3(2) — the oracle's sort, cited in full** [AC-1]:

```
sprite.cpp:41-48   static bool sortSpriteHelper(const Sprite &e1, const Sprite &e2) {
                       if (e1.sortOrder == e2.sortOrder)
                           return e1.givenOrderNr < e2.givenOrderNr;   // STABLE
                       return e1.sortOrder < e2.sortOrder; }           // ASCENDING
sprite.cpp:63      Common::sort(_spriteRegularList.begin(), ..., sortSpriteHelper);
sprite.cpp:80      Common::sort(_spriteStaticList.begin(),  ..., sortSpriteHelper);
sprite.cpp:97-103  if (screenObj->flags & fFixedPriority)
                       spriteEntry.sortOrder = _gfx->priorityToY(screenObj->priority);
                   else
                       spriteEntry.sortOrder = screenObj->yPos;
graphics.cpp       priorityToY(p) default = (p - 5) * 12 + 48          // monotonic in p
```

★★★ **So "back to front" in the oracle's own terms is ascending `sortOrder`**, and because
`priorityToY` is monotonic, sorting on `sortOrder` orders by **depth band** whether an object's
priority is fixed or derived from its Y.

★★★★★ **Two structures the dispatch does not mention, and one is a gap I did not close.**
`buildAllSpriteLists` builds a **static** list (`fAnimated|fDrawn`, no `fUpdate`) and a **regular**
one (`fAnimated|fUpdate|fDrawn`), sorts each, and builds **static first** [`sprite.cpp:67-86`].
★★★★ **Our `p3_spr` is one flat list keyed on `fDrawn` alone**, so a static sprite is not guaranteed
to precede a regular one. **Not implemented; recorded in §7(2).**

**§3(3) — every reader of the priority plane, by routine** [AC-3]:

| reader | reads | needs SPRITE priority? |
|---|---|---|
| `co_depth` [`composite.s:390`] | the plane, per opaque pixel | ★★★ **yes** — it is the depth test's subject |
| **`co_checkctrl`** [`composite.s:714`] | walks **down** a column for the first value > 2 | ★★★★★ **YES** — see below |
| `co_pix`/`co_opaque` [`:296, :336`] | the plane, to set up the test | via `co_depth` |
| `co_save`/`co_restore` [`:714`-region] | — | ★ **dead code**; `CP_SAVE` is defined nowhere [P6.56] |
| `pic_core.s`, `pic_fill.s` | **write** the PICTURE's priority at room render | no |
| `p3_restore_prev` | **writes** the plane back from the shadow | no |
| the VM phase | ★★★★ **cannot** — slot 5 holds `VM_OBJ`/vocabulary then, not the priority slice [`MAP_VOCAB equ MAP_PRI_SLICE`] | **n/a** |

★★★★★ **The VM-phase reader class §1.3 worried about does not exist in this probe**: the priority
plane is not mapped during `interpret`, so `obj.in.box`, the position tests and anything in
`vm_objects.s` physically cannot see a sprite stamp. ★★★★ **`co_checkctrl` is the one that kills it**
— `checkControlPixel()` walks **downward** from the current pixel, and sorting puts earlier sprites
*above*, so most of the time the walk misses them; **but a tall later sprite's upper rows lie above
an earlier sprite's bottom rows**, and the walk from there reads a stamp.

**§3(4) — `co_depth`'s two `plane_pri` calls.** P6.87 cached the address between them. ★★ **Nothing
was removed, so both remain**, and the cached address is still reused; `-DCOMP_PRIX_REDERIVE` is the
before arm and is untouched.

**Authority tier.** §4A's citation is **ScummVM at the pin, tier 3** [§2]. ★★★ Per §2.1 I state
which: **the sort is believed ORIGINAL, not a ScummVM normalisation** — `givenOrderNr` exists only to
make an unstable `Common::sort` behave like the original's in-place walk, which is a fact about C++
rather than about AGI, and the `priorityToY` block carries a comment about reproducing a
*version-specific glitch* in KQ4 2.0, which is what a faithful reimplementation does rather than a
normalising one. ★★ **The stamp is likewise believed original**: `checkControlPixel` requires it.

**§2H's three checks.** (1) *A second mechanism for a different object class?* ★★★★★ **Yes, and it
is §3(2)'s gap**: static and regular sprites are two lists with two sorts and a defined order
between them. (2) *The calling routine.* `sortSpriteHelper` is called by `Common::sort` from
`buildRegularSpriteList`/`buildStaticSpriteList`, which run **per cycle before `drawSprites`** —
so the sort belongs in staging, which is where it went. (3) *Grep the reports.* P6.89 (99 of 99
isolated), P6.91b (`p3_stage_sprites` has no sort — this task's origin), P6.93 (the cache) agree;
**P6.91b's §8(3) asked for exactly this ruling and it is now answered.**

**§2S.** No sibling claim is made.

### 4 — Verification (AC-by-AC)

- **AC-1 [class: citation · oracle]** **PASS** — §3(2), with the key, the order, the tie-break and
  the equal-priority behaviour, cited by file and line.

- **AC-2 [class: state-comparable]** **PASS.** Both planes byte-identical to P6.93's build over 120
  cycles:

  | | visual | priority |
  |---|---|---|
  | standing | `A98D101961D695FA` = P6.93 | `AC8216D4CFD8E491` = P6.93 |
  | moving | `017E2FA895B56EA1` = P6.93 | `F6A6C4B658928EFF` = P6.93 |

  ★★★★ **And the sort is NOT a no-op** — §2W, checked before the identity was believed: the staged
  order changed from `[ego y100, v97 y17, v107 y161, v107 y135]` to
  `[ego y100, v107 y135, v107 y161, v97 y17]`. **No pixel moved because the castle's sprites do not
  overlap**, which §6's third trigger asked about and which is a fact about the scene, not the sort.

- **AC-3 [class: measurement]** **PASS** — §3(3)'s table. ★★★★★ **Decision: the write STAYS**, on two
  independent grounds (oracle fidelity, `sprite.cpp:350`; and `co_checkctrl` as a live reader).
  ★★ **No reader was weakened to make it removable** [§4C].

- **AC-4 [class: measurement]** **PASS.**
  1. **Standing 0.2003 s/cycle (4.99/s); moving 0.2336 (4.28/s)** — **unchanged from P6.93 to the
     digit**, as a fidelity-only change should be.
  2. **`cp_composite`'s share — not re-profiled.** ★★★ Nothing in the blit changed; the write stayed
     and no instruction was added or removed inside `co_row`. §7(4).
  3. **Stores per drawn pixel: UNCHANGED.** The claim was one store and one address computation;
     **neither went**, and §4C says why.
  4. ★★★★ **The sort's own cost is invisible**: the `sprites` stage is **0.00281 s/cycle, 1.0%** of
     the cycle, and the medians did not move. An insertion sort over 4 entries is a few hundred
     cycles against a 0.2 s cycle — **below the measurement's resolution**, which is what §4D(4)
     asked to be shown rather than asserted.
  5. **Room change and room 83 — not re-measured.** §7(4).

- **AC-5 [class: state-comparable · fault injection]** ★★★★★ **PASS, but only in a constructed
  scene, and that is the finding.**

  | arm | visual | priority |
  |---|---|---|
  | `-SortReverse`, castle | `017E2FA895B56EA1` — **IDENTICAL** | `F6A6C4B658928EFF` — **IDENTICAL** |
  | `-ForceOverlap` | `B9A082E9B897E79C` | `F6A6C4B658928EFF` |
  | `-ForceOverlap -SortReverse` | **`D7876E40CCB30328`** | **`97DEA0CF359FBEFC`** |

  ★★★★★ **Reversing the sort in the castle changes nothing** — draw order is unobservable where
  nothing overlaps. ★★★★ **The arm is only red under a forced overlap.** ★★★ **So our gated corpus
  cannot detect a draw-order defect at all**, which is precisely how the missing sort survived from
  the day staging was written [L-86's shape: a fixed sample that always passes is evidence about the
  sample first].

- **AC-6 [class: byte-comparable · gate]** **PASS, fresh, exit 0.** `comp` **124/124**, `cel`
  **9,193/9,193**, `res` **1,264/1,264**, `pic` 45/45, five p3b probes — *"all green"*.
  ★★★ **`comp_probe` and `cel_probe` byte-identical** (`probe_identity_check`), **even though the
  sort is unconditional** — because it is in `p3b_probe.s`, not in `composite.s`, whose only change
  is a comment. ★★ `vm` not re-run; no VM path changed.

- **AC-7 [class: state-comparable]** **PASS.** `-ForceOverlap` still drives `isolated` from **99 to
  0** — P6.88's guards still refuse every skip. ★★★★★ **§4E's answer: the guard is NOT redundant.**
  Its reasoning rests on draw order being arbitrary and it no longer is — **but the hazard is the
  SKIP, not the order.** Skipping i removes it from the sequence entirely, so an overlapping j at
  equal priority still wins where a full redraw would have put i on top. **Sorting makes "which is
  later" defined; it does not make it irrelevant.** Not touched [§10].

- **AC-8 [class: suite]** **PASS.** All five p3b probes green. Fault arms red under their own
  scenarios: `-SortReverse -ForceOverlap` (AC-5), `p3b_arms_check -SelfTest` red as designed.

- **AC-9 [class: eye-gated — Jay]** ★★★★★ **PASSED — Jay, poke, RGB.** Offered **before** the byte
  gates were reported [§4A]. Jay: **"1.yes 2.same 3.no"** — sprites layer correctly; no speed
  change; nothing wrong. ★★★★ **All three match the expectations given in the ask**: I predicted no
  visible change (planes byte-identical), no speed change (the write stayed), and said so before he
  looked.

- **AC-10 [class: manifest]** **PASS — re-baselined, and every arm moved.**

  | arm | was | now | Δ |
  |---|---|---|---|
  | p3b | 16,114 `8548D2A5` | **16,292 `2062158E`** | +178 |
  | p3b_text | 16,899 `D1D65E0A` | **17,075 `80527351`** | +176 |
  | p3b_win3 | 16,899 `4A183DF1` | **17,075 `5CDE2845`** | +176 |
  | p3b_notick | 16,896 `841BC1AD` | **17,072 `06B965E2`** | +176 |
  | p3b_nomap | 16,896 `2A298702` | **17,072 `E5F90993`** | +176 |
  | p3b_fault | 15,945 `4A31BB0D` | **16,121 `B56E36AD`** | +176 |
  | p3b_flat | 15,930 `18B0E2FB` | **16,106 `78CFE4B7`** | +176 |
  | p3b_comb | 19,336 `B5A60E1D` | **19,514 `F7E97915`** | +178 |
  | p3b_comb_count | 19,371 `A3DD030C` | **19,549 `FC00E08F`** | +178 |

  ★★★ **+178 on the three cel arms** — they also carry `pca_lp`'s `leay` over the seventh byte.
  ★★ **Region A: `P3_CODE_END` `$5F69`, 151 bytes to `$6000`** (was 223 at P6.93). The retired
  figures are kept in the file.

- **AC-11 [class: tooling]** **PASS.** `hal_sync_check` OK (11 files, 3 repos); `reg_discipline`
  **10 accesses, 1 file, 4 registers — one owner**; `gen_vm_tables --check` OK;
  `probe_identity_check` every non-p3b probe byte-identical; `p3b_arms_check -SelfTest` red.

- **AC-12** **PASS.** §10.

### 5 — Verdict-time evidence (v0.7 §11)

```
=== AC-1, the oracle's sort (engines/agi/sprite.cpp at the pin) ===
41: static bool sortSpriteHelper(const Sprite &entry1, const Sprite &entry2) {
42:     if (entry1.sortOrder == entry2.sortOrder) {
45:         return entry1.givenOrderNr < entry2.givenOrderNr;      <-- STABLE
47:     return entry1.sortOrder < entry2.sortOrder;                <-- ASCENDING
63:     Common::sort(_spriteRegularList.begin(), ..., sortSpriteHelper);
80:     Common::sort(_spriteStaticList.begin(),  ..., sortSpriteHelper);
97:     if (screenObj->flags & fFixedPriority)
98:         spriteEntry.sortOrder = _gfx->priorityToY(screenObj->priority);
101:        spriteEntry.sortOrder = screenObj->yPos;

=== AC-3, why the stamp stays (sprite.cpp / graphics.cpp) ===
350:    _gfx->putPixel(curX, curY, GFX_SCREEN_MASK_ALL, curColor, viewPriority);
        void GfxMgr::putPixel(...) { ...
            if (drawMask & GFX_SCREEN_MASK_PRIORITY) { _priorityScreen[offset] = priority; } }
   -- MASK_ALL includes PRIORITY: the oracle stamps every drawn pixel, as we do.

=== AC-2 / §2W, the sort is not a no-op: staged order before -> after ===
before  [0] ego  y=100 prio=9    [1] v97  y= 17 prio=15
        [2] v107 y=161 prio=14   [3] v107 y=135 prio=12
after   [0] ego  y=100 prio=9    [1] v107 y=135 prio=12
        [2] v107 y=161 prio=14   [3] v97  y= 17 prio=15
   -- keys 100, 135, 161, 168 ascending. v97 has fFixedPriority, so its key is
      priorityToY(15) = (15-5)*12+48 = 168 and it moves from index 1 to LAST.

=== AC-2, planes ===
P6.93 standing  A98D101961D695FA / AC8216D4CFD8E491
SORTED standing A98D101961D695FA / AC8216D4CFD8E491      IDENTICAL
P6.93 moving    017E2FA895B56EA1 / F6A6C4B658928EFF
SORTED moving   017E2FA895B56EA1 / F6A6C4B658928EFF      IDENTICAL

=== AC-5, the fault arm CANNOT fire without an overlap ===
-SortReverse, castle            017E2FA895B56EA1 / F6A6C4B658928EFF   <-- IDENTICAL
-ForceOverlap                   B9A082E9B897E79C / F6A6C4B658928EFF
-ForceOverlap -SortReverse      D7876E40CCB30328 / 97DEA0CF359FBEFC   <-- RED

=== AC-4, timing (P6.93: 0.2003 standing, 0.2336 moving) ===
SORTED standing  median 0.2003 s/cycle = 4.99 cycles/second
SORTED moving    median 0.2336 s/cycle = 4.28 cycles/second
sprites stage    0.00281 s/cycle   1.0%   (120 entries)  <-- the sort is inside this

=== AC-7 / §4E ===
-ForceOverlap : SKIP ceiling unchanged 66 (0.55/cycle), of which isolated 0 (0.00/cycle)
baseline      : SKIP ceiling unchanged 110 (0.92/cycle), of which isolated 99 (0.82/cycle)

=== AC-6 ===
★ source integrity: clean
TOTAL (res) 1264 1264 1264 0 0      TOTAL (cel) 9193 9193 9193 0 0 1525
Kingquest3-v0 (comp) 124 124 124 0 0
★ gates run: pic res cel comp p3b p3b_text p3b_box p3b_parse p3b_row22  -- all green
gates exit=0

=== AC-10 / AC-11 ===
★ all 9 arms byte-identical to the recorded baseline (SHA256)   (after re-baselining)
★ 1 ARM(S) MOVED                                                (-SelfTest, the intended red)
P3_CODE_END = 5F69  -> 151 bytes to $6000   (was 223 at P6.93)
[reg-discipline] 10 register access(es) in 1 file(s) over 4 register(s).
[hal-sync] OK (11 files compared)   CHECK OK: vm_tables.s matches optable.py
★ every non-p3b probe byte-identical      mojibake: clean, exit=0
```

**25.2 bundled-artifact grep:** N/A — nothing bundled.

**25.3 operator-runtime-smoke:** ★★★★★ **PASSED — Jay, poke, RGB**, offered before the byte gates.
*"1.yes 2.same 3.no"*.

### 6 — Reactive deviations and route accounting

1. ★★★★★ **§6's second trigger fired: a reader needs sprite priority.** The task delivered **the
   sort alone** and kept the write, as that trigger instructs. ★★★★ **And the fidelity citation is
   the stronger half** — even with no reader, `sprite.cpp:350` forecloses removal.
2. ★★★★ **The oracle's sort key is not what the dispatch assumed.** §1.1 and §4A speak of "back to
   front" and priority ordering; the key is **`yPos`/`priorityToY`**. ★★★ Implemented to the
   oracle's rule, not the dispatch's description.
3. ★★★ **`P3_SPR_SIZE` went 6 → 7 and the host had the stride hardcoded at FOUR places.** Replaced
   with one `SPR_SIZE` constant. ★★★★ **A missed site would not have failed** — it would have read
   the neighbouring sprite's bytes shifted by one and printed a plausible row.
4. ★★ **`pca_lp` needed `leay P3_SPR_SIZE-6,y`.** Without it every sprite after the first reads one
   byte skewed. Spelled from the constant so it cannot drift.
5. ★ **The static/regular two-list split was NOT implemented** — §7(2).

**ROUTE ACCOUNTING.** I proposed to Jay, before he looked: no visible change (planes byte-identical),
no speed change (the write stayed), and that the sort's value would only show in a room with
overlapping sprites. ★★ **All three held** — *"1.yes 2.same 3.no"*. **What I did not build**: the
removal of the priority write, the static/regular split, and any change to P6.88's guard.

### 7 — Uncertainty flags

1. ★★★★★ **OUR CORPUS CANNOT SEE DRAW ORDER.** AC-5's arm is invisible without `-ForceOverlap`, and
   `comp`'s 124/124 scenes have no overlaps. ★★★★ **So the sort is correct by citation, not by
   gate** — the only byte evidence that it is *right* rather than merely *different* is that it
   changes nothing where nothing overlaps. **A gated scene with overlapping sprites at known depths
   is the missing instrument.**
2. ★★★★★ **The static/regular split is not implemented.** The oracle keeps two lists and draws
   static before regular [`sprite.cpp:67-86`]; we keep one keyed on `fDrawn`. ★★★ **If a room stages
   a static and a regular sprite that overlap, our order may still differ from the oracle's.**
3. ★★★★ **`priorityToY`'s table form is not modelled.** We use the default `(p-5)*12+48`; a game
   that sets a priority table gets a table-driven mapping, and ScummVM reproduces a
   **version-specific glitch** there for KQ4 2.0. **Our pinned set may or may not exercise it.**
4. ★★★ **AC-4(2) and (5) not measured** — `cp_composite`'s share and the room-change/room-83
   figures. Nothing in the blit changed, so no movement is expected, but it is not shown.
5. ★★★ **Region A is at 151 bytes.** This task took 72 in the shipped arm; P6.93 took 558.
   **The next addition to p3b meets that**, and two diagnostic arms are already blocked.
6. ★★ **The sort is stable by construction** (shift only on strict `>`), matching `givenOrderNr`.
   **Not separately tested with equal keys**, because the castle has none.

### 8 — Follow-up candidates

1. ★★★★★ **A gated scene with overlapping sprites** — §7(1). **This is the instrument the project
   lacks**, and it would have caught the missing sort years of tasks ago. It also unblocks testing
   P6.88's guard, `co_depth`'s rejection path (`co_rej_pri` is 0 in the castle) and the static/regular
   order.
2. ★★★★★ **Put the cel cache in the gated arms** — P6.93 §7(1), **still open across three tasks**.
   The arms check builds without `-DCEL_CACHE`, so no gate compiles it.
3. ★★★★ **The static/regular split** — §7(2).
4. ★★★★ **The blit is still the largest item** and this task did not touch it: `cp_composite` ~29% of
   the drawing stage. ★★★ **P6.94's verdict stands** — we are ~6× more sensitive to animation load
   than Sierra — **and the priority stamp is now known to be load-bearing, so the win is elsewhere
   inside that loop.**
5. ★★★ **Region A / the map ruling** — §7(5).
6. Carried: the VIEW checksum gap; design spec §7.1's `0.039 s/cycle`; option 3 and the sidecar;
   real-time pacing; `checkPriority`/`checkCollision`; `MAP_PRI_BANDS` (fortieth task — ★
   `memmap.inc` is a §6 stop trigger, not bumped).

### 9 — User interaction during task

1. **Eye gate offered before the byte gates** [§4A], with expectations stated, including the unusual
   one: *expect no visible change, and here is why*. Jay: **"1.yes 2.same 3.no"**.

### 10 — Candidate(s) captured this task
`seeds/AGI/live/2026-09-24-the-corpus-cannot-see-the-defect-class.md`

### 11 — Commit
(recorded below after push)
