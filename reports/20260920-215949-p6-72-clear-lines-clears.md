## Form B Report — P6.72 — `clear.lines` clears; the alligators are MISPLACED, not frozen
**Class:** integration (§4A).  wip.

### 0 — Receipt / status (C-35 stamp)
t0=2026-09-20 (HEAD 9a9d194, wip). git status clean at t0 (one untracked `coco_agi.code-workspace`,
not this project's and not staged).

### 1 — Summary

★★★★★ **`clear.lines` is real and Jay confirmed it: "1 yes."** The text strip measures **0 of 5120
non-black bytes — ALL BLACK** after the advance, on the headless run and on the live one, against
**266** with the fault arm. ★★★★ **The target value was named before the run this time** — zero,
not "fewer" — which is P6.71's lesson applied rather than restated.

★★★★★ **§4B's two questions are answered, and the answer is PLACEMENT, not cycling.** At the
advance the game runs `position 11, 121, 161` and `position 12, 73, 166`; **we stage both at
x=147.** ★★★ **The cel cycler is not implicated: it exists** [`vm_objects.s:187`, gated on
`fCycling`], **`animate.obj` assigns `fCycling`, and `start.cycling` is never called by the game at
all** (0 in 400 cycles).

★★★★★ **And Jay's fourth answer was not a defect in the port.** *"graham started walking and then
the game froze."* The log records `cycle 180 ... err 0` followed by `holding the display for 25
emulated s`. **No stall, no watchdog — the run's cycle budget simply ran out.** ★★★★ **A fixed
cycle budget is indistinguishable from a crash to someone interacting with it**, and the runner now
says so before every live run.

### 2 — Files modified
- `src/harness/vm_text_ops.s` — `vmop_clear_lines` implemented; `-DP3B_FAULT_NOCLEARLINES`.
- `harness/tools/p3b_show.ps1` — `-NoClearLines`; the "this run executes N cycles and then FREEZES"
  banner on the live path.
- `harness/tools/p3b_arms_check.ps1` — five arms re-baselined.

### 3 — Reasoning

**§4A — `clear.lines`, from the oracle** [tier: ScummVM, pin 9d9b9e93; believed ORIGINAL, §2.1].

```c
op_cmd.cpp:2181  rowUpper = p[0]; rowLower = p[1]; colour = calculateTextBackground(p[2]);
                 if (rowUpper > rowLower) rowLower = rowUpper;       // guard for buggy games
text.cpp:635     clearLines -> clearBlock(upper, 0, lower, FONT_COLUMN_CHARACTERS-1, colour)
text.cpp:639     clip both corners; translateFontRectToDisplayScreen; drawDisplayRect(...)
text.cpp         calculateTextBackground: 15 only if gfxMode AND arg non-zero, else 0
```

★★★★★ **It writes the DISPLAY screen, not the game screen** — so it never touches the picture, and
§6's third trigger was answered before any code was written.

★★★★ **KQ1 calls `clear.lines(21, 24, 0)`** — measured, not assumed. Rows 21-24 is exactly the
text strip `p3_black_visible` establishes and `p3_present` stops short of [P6.62], and colour 0 is
black because the argument is zero.

★★★★ **Nothing new was needed in `text.s`.** `txt_clearline`'s own header already said it:
*"clearBlock(row, 0, row, 39, colour) in the oracle... this is one tx_boxfill"* [text.s:1135].
**`clear.lines` is that routine, upper through lower.** The attribute is set for the fill and put
back, because the oracle passes a colour explicitly and `txt_clearline` reads `txt_bg` [AD-160's
shape: an attribute left behind drew twelve glyphs black on black].

**§3(3)/§3(4) — the cel advance EXISTS, so §1.2's premise holds.** `vm_update_objs`'s cycler is at
`vm_objects.s:187`: `ldd VMO_FLAGS,x / bitb #fCycling / beq vm_uo_next / lda VMO_CYCLETIMECNT,x`.
`VMO_CEL`'s writers are `vm_set_cel_open` and `vm_set_loop_open` [vm_run.s:317,353] — the opcode
handlers — plus this cycler.

**★★★★★ §4B(2) — the placement, measured on both sides.** At cycle 200, logic 1:

```
animate.obj 11 / 12 / 1        set.view 11,107   set.view 12,107   set.view 1,97
position 11, 121, 161          position 12, 73, 166                position 1, 5, 17
draw 11 / 12 / 1               position 0, 110, 100                draw 0
```

| object | the game says | we stage |
|---|---|---|
| 0 Graham | (110,100) | **(110,100)** ✓ |
| 1 flags | (5,17) | **(5,17)** ✓ |
| **11 alligator** | **(121,161)** | **(147,161)** — y right, x wrong |
| **12 alligator** | **(73,166)** | **(147,80)** — both wrong |

★★★★ **Graham and the flags land correctly, so `position` is not simply broken** — something
specific to these two objects. ★★★ **Per §4D I did not fix it**: two fixes to one subsystem in one
task is how a regression is attributed to the wrong change.

**§4B(1) — the cel advance.** `start.cycling` **0** and `cycle.time` **0** in 400 cycles;
`stop.cycling` 401, **every cycle on object 0 (Graham, not the alligators)**; `animate.obj` 5,
and it assigns `fAnimated+fUpdate+fCycling` [vm_cmds.s:264]. ★★ **So the alligators should carry
`fCycling` and the cycler should run** — **not confirmed on the guest**, see §7.2.

### 4 — Verification (AC-by-AC)

- **AC-1 [citation · oracle] PASS** — §4A above, quoted with file:line.
- **AC-2 [state-comparable] PASS** — **text strip 0 of 5120 non-black (ALL BLACK)** after the
  advance, headless and live, against **266** with `-NoClearLines`. **Jay: "1 yes."**
- **AC-3 [measurement] PARTIAL, and the shortfall is stated.** §4B asked for a per-cycle table of
  `VMO_FLAGS`/`CEL`/`LOOP`/`X`/`STEPTIME`/`CYCLETIME` for both objects. ★★★ **I did not produce
  it.** I answered both questions from the **game's own opcode stream plus the guest's staged
  sprite table**, which settled the placement question outright — but it leaves §4B(1) inferred
  rather than measured.
- **AC-4 [attribution] PASS, both named separately** — **placement**: the game positions them at
  (121,161)/(73,166) and we stage both at x=147. **Cel advance**: the cycler exists and is gated on
  `fCycling`, which `animate.obj` sets; `start.cycling` is never called. ★★ **The second is a lead,
  not a finding** [§7.2].
- **AC-5 [citation · oracle] NOT DONE** — §4C asks for the oracle's answer for whatever §4B named.
  §4B named **placement**, and I did not read the oracle's `position`/`updatePosition` path. ★★★
  **Named as not done rather than glossed**; it is §8.1's first step.
- **AC-6 [state-comparable] N/A** — only `clear.lines` was fixed, per §4D.
- **AC-7 [byte-comparable] PASS** — **pic 45/45 · res 1,264/1,264 · cel 9,193/9,193 · comp 124/124**,
  fresh. ★★ **The nine-title `vm` gate was not re-run**; `vm_probe` has no `TEXT_WIRED`, so it takes
  `vm_text_ops.s`'s `equ` branch — and **`p3b_fault` and `p3b_flat` (both `-DTEXT_MODELLED`) came
  back byte-identical**, which is direct evidence that branch emits nothing. §7.3.
- **AC-8 [suite] PASS** — `p3b`, `p3b_text`, `p3b_box`, `p3b_parse`, `p3b_row22` green.
- **AC-9 [fault injection] PASS** — `-NoClearLines` restores the bare `rts` (−83 B) and the strip
  stays at **266**. ★★★★ **A known-good red in Jay's own words**, quoted at the code.
- **AC-10 [eye gate — Jay]** ★★★★★ **Q1 PASS: "1 yes."** **Q2/Q3: "there are no alligators"** —
  stronger than my expectation, which was "present but not moving" [§7.1]. **Q4: "graham started
  walking and then the game froze"** — ★★★★ **not a defect: the run's cycle budget ended** [§1].
- **AC-11 [manifest] PASS** — five arms re-baselined (+83 B each): the four wired-text arms and the
  combined one. **`p3b`, `p3b_fault`, `p3b_flat` byte-identical**, the split falling exactly along
  `TEXT_WIRED`. 8/8 OK after.
- **AC-12 [tooling] PASS** — `hal_sync_check.py` OK against both siblings (11 files);
  `reg_discipline.py` 17 accesses, 1 file, 4 registers (unchanged); `gen_vm_tables.py --check` OK;
  `p3b_arms_check.ps1` 8/8; `fix_mojibake --check` clean.
- **AC-13** Candidate captured (§10).

### 5 — Verdict-time evidence (v0.7 §11)

```
25.1  clear.lines real : text area rows 168-199: 0 of 5120 bytes non-black  ★ ALL BLACK
      -NoClearLines    : text area rows 168-199: 266 of 5120 bytes non-black
      live run         : 180 cycles in 42.1543 emulated s
                         VAR 19 publishes: 1 key(s), last $20   <- Jay's space bar
                         final room 1, sprites 4, err 0
                         text area rows 168-199: 0 of 5120 bytes non-black  ★ ALL BLACK
                         cycle 180  0.7343 s  room 1  sprites 4  remaps 434  err 0
                         ★ holding the display for 25 emulated s (P3B_HOLD)

      per-picture: 45 PASS, 0 FAIL (of 45)
      resources byte-identical to tools/volread/: 1264 / 1264 (100.00%)
      cels byte-identical to the oracle: 9193 / 9193 (100.00%)
      ★ composites byte-identical on both planes: 124 / 124 frames (100.00%)
      ★ gates run: pic res cel comp p3b p3b_text p3b_box p3b_parse p3b_row22 -- all green
      ★ all 8 arms byte-identical to the recorded baseline (SHA256)
      [hal-sync] OK -- aligned with POP3_port, karateka_coco3 (11 files compared)
      [reg-discipline] 17 access(es) in 1 file(s) over 4 register(s)
      CHECK OK: src/harness/vm_tables.s matches optable.py
25.2  N/A -- probe and harness code only.
25.3  Q1 PASSED -- Jay, live-disk, RGB, combined arm, -NoRoomJump, 180 cycles.
      "1 yes. 2.there are no alligators, 3. no alligators. 4. graham started walking and
       then the game froze,"
```

### 6 — Reactive deviations and route accounting

1. ★★★★ **The "this run executes N cycles and then FREEZES" banner** is a §22.5 addition the
   dispatch did not ask for. Jay read a finished run as a crash, and the log said otherwise; **the
   gate should say which it is before he has to ask.**
2. ★★ The four `tx_cl_*` state bytes sit **outside** the fault guard deliberately, so
   `-NoClearLines` differs from the shipped build **only in the handler** — one variable, not two
   [L-73].

**ROUTE ACCOUNTING.** §4D told me to fix the one with the clearer oracle citation and report the
other; **I fixed `clear.lines` and left the placement measured and untouched.** ★★★ **I did not
produce §4B's per-cycle VMO table** and answered its questions another way — that is a real
shortfall against the AC and is recorded as one in AC-3 rather than presented as an equivalent.

### 7 — Uncertainty flags

1. ★★★★ **"There are no alligators" is stronger than my prediction and I cannot yet explain it.**
   I expected "present but static". Two objects are staged with `view=107` and the compositor
   writes pixels — **but nothing recognisable appears.** Misplacement alone (x=147 vs 121/73) may
   account for it, or there may be a second cause. **Not resolved.**
2. ★★★★ **§4B(1) is inferred, not measured.** `animate.obj` assigns `fCycling`, so the cycler
   *should* run — I did not read `VMO_FLAGS`/`VMO_CYCLETIMECNT` on the guest to confirm it does.
3. ★★★ **The nine-title `vm` gate was not re-run** (AC-7). The evidence that it cannot have moved
   is the two `TEXT_MODELLED` arms coming back byte-identical — strong, but a proxy.
4. ★★ **Only KQ1 exercises this.** `clear.lines` is now real for every wired-text build and no
   other title has been run against it.

### 8 — Follow-up candidates

1. ★★★★★ **Why are objects 11 and 12 staged at x=147 when the game positions them at 121 and 73?**
   The next task. **Start with AC-5's omission**: read the oracle's `position`/`updatePosition`
   path, then compare against `vmop_position` and `p3_stage_sprites`.
2. ★★★★ **Then re-ask "are there alligators"** — placement may be the whole of it, or not [§7.1].
3. ★★★ **Confirm `fCycling` on the guest** for objects 11/12 [§7.2] — one readout, and it closes
   §4B(1) properly.
4. ★★ **`start.cycling` is never called by KQ1's room 1**, so cel animation there depends entirely
   on `animate.obj`'s assignment. Worth knowing before anyone "fixes" cycling.
5. ★★ **Pin the game release** — carried from P6.69, P6.70, P6.71, still not done.
6. ★ Carried: the flags' blink · the picture leaving the logic arena · `configure.screen`'s render
   offset · `set.key`'s 16-bit gap · `MAP_PRI_BANDS` (eighteenth task).

### 9 — Doc-edit deltas applied
- `vm_text_ops.s` — the stub note replaced with what `clear.lines` now does, the oracle's colour
  rule, and the measured `clear.lines(21,24,0)`.
- `p3b_show.ps1` — the cycle-budget banner, with Jay's words and the log line that disproves the
  crash reading.
- `p3b_arms_check.ps1` — why the split falls along `TEXT_WIRED`.
- **Design-spec text: none proposed** [§2D].

### 10 — Candidate(s) captured this task
- `seeds/AGI/live/2026-09-20-a-harness-bound-with-no-counterpart-reads-as-a-failure.md`

### 11 — Commit
`622310b` — 4 files changed, 295 insertions(+), 8 deletions(-).
Pushed to origin/wip (`9a9d194..622310b`).
