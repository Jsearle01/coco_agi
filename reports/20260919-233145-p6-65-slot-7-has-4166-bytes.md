## Form B Report — T-P0-119 / P6.65 — Slot 7 has 4,166 bytes, and the aperture cannot move
**Class:** measurement.  wip.  **No `src/` change.**

### 0 — Receipt / status (C-35 stamp)

t0=2026-09-19 23:31:45 (HEAD ec6cb09, wip). ★★★★★ **`git diff --stat -- src/` is EMPTY** (AC-7);
`git status` shows only the editor's untracked `coco_agi.code-workspace`.

### 1 — Summary

★★★★★ **Shape 1 is CLOSED and shape 2 is WIDE OPEN, and both answers come from the same table.**

**The aperture's place is forced** — not by the 10,964-byte LOGIC, which forces its *size*, but by
**every one of the eight MMU slots being allocated** (§3.1). There is nowhere to move it to.

★★★★★ **The same table shows slot 7 is never remapped either — and it has a 4,166-byte hole.**
`$EBBA-$FC00`, between the font's end and `MAP_COVERAGE`, **unclaimed in all three arms**, in a slot
that already holds the parser as code and the font as data. **513 B are needed.**

★★★★ **`$44` was never lost.** It is gated on `SYM.txt_bgx` and on a message box being on screen —
**T-P0-088's box-contamination diagnostic**, not a suite row. §3.5. ★★★ **Carrying it in six AC
lists was the error; it should be retired from them.**

★★ **Nothing was built.** §7A requests the ruling.

### 2 — Files modified

**None.**

### 3 — Reasoning

#### 3.1 §4A / AC-1 — the enumeration, and AC-3's answer falls straight out of it

`memmap.inc:19-28`, the probe's own phase table:

```
 slot  window        VM phase                    draw phase
  0    $0000-$1FFF   system + VM state + DIRs    unchanged -- always resident
  1    $2000-$3FFF   engine code                 unchanged
  2    $4000-$5FFF   engine code + reserved      unchanged
  3    $6000-$7FFF   ARENA window lo             unchanged
  4    $8000-$9FFF   ARENA window hi             unchanged
  5    $A000-$BFFF   -- nothing mapped --        PRIORITY slice   (VM phase: VOCABULARY)
  6    $C000-$DFFF   VOLUME window               FRAMEBUFFER slice
  7    $E000-$FEFF   resident tables             unchanged
```

★★★★★ **AC-3: the aperture's PLACE is forced, and by exhaustion.** Its *size* is forced by
measurement — *"a one-slot window could not address the largest LOGIC in seven of seven titles"*
[`memmap.inc:240`] — but its *place* is forced by there being **no pair of adjacent slots free to
move it to**. Slot 5 is the priority slice (and the vocabulary in the VM phase), slot 6 is the
volume/framebuffer window, slot 7 is resident tables. ★★★ **Every slot has an owner and the arena
needs two adjacent ones.**

★★★ **§1.1's reading — "the arena is windowed, so the question is whether the aperture can move" —
is right about the mechanism and closed by the allocation.** Recorded as the answer rather than left
as a maybe, which is what §4A asked for.

★★ **And a correction to the dispatch's citation:** the arena-occupancy figure (15,376 of 16,384,
1,008 free, park-sampled) is **P6.49's**, not P6.57's. It bears on shrinking residency, which is not
a shape here.

#### 3.2 ★★★★★ AC-4 / shape 2 — slot 7 has a 4,166-byte hole

Region B's occupancy, measured from the combined arm's map:

```
$E000-$E366   parser, 954 B      [P3_PARSER_BASE .. P3_PARSER_END]
$E366         P3_INBUF           $E390  P3_CLNBUF
$E3BA-$EBBA   font, 2,048 B      [P3_FONT + P3_FONT_BYTES]
$EBBA-$FC00   ★★★★★ NOTHING -- 4,166 bytes
$FC00-$FE00   MAP_COVERAGE       (TESTSEEN/OPSEEN, 512 B, -DP3B_COVERAGE only)
$FEF0         P3_REGIONB_END     (the IRQ vector stubs)
```

★★★★ **Verified in THREE arms, not one** — cel, text and combined all report **zero symbols** in
`$EBBA-$FC00`. ★★★ **That is the discipline P6.64 corrected me on**: one measurement of the quantity
that matters, repeated across the configurations that could disagree.

★★★★★ **Slot 7 is the right kind of space, not merely space.** It is *"resident tables ...
unchanged"* in both phases — **never remapped**, which is the property code needs and the property
`MAP_INPUT` was chosen for at T-P0-118. ★★★ **It already holds code**: the parser is `org`ed at
`$E000` and runs 954 bytes. **This is not a new idea, it is the same idea the font used at P6.36.**

★★★★ **And the mechanism exists.** The image is already four runs and `p3b_run.lua:702` pokes N runs
keyed on symbols [P6.64 §3.5]. **A fifth run costs the host nothing.**

#### 3.3 §4A / AC-2 — the three shapes

| | 1. move the aperture | 2. code into slot 7 | 3. shrink 513 B |
|---|---|---|---|
| **bytes gained** | — | ★★★★★ **4,166** | 513, by construction |
| **status** | ★★★★ **CLOSED** — every slot is allocated (§3.1) | ★★★★★ **open** | open |
| **displaces** | would have to displace priority, the volume window, or resident tables | **nothing measured** | nothing |
| **mechanism** | new | ★★★★ **exists** — `org` + a poke run, as `vm_tables` and the parser already do | none needed |
| **risk** | — | ★★★ a `jsr` from region A to `$Exxx` is ordinary extended addressing; the slot is resident in both phases | ★★★ this arc re-consumed region A in **seven of ten tasks** |
| **forecloses** | — | 513 of 4,166; **3,653 left** | nothing, and buys nothing durable |
| **gated by** | — | §3.4 | §3.4 |

★★★ **Shape 3 is not wrong, it is temporary.** P6.63's own finding was that space freed gets spent
by the work that freed it; 513 B of shrink would be consumed by the next two features on the same
evidence.

#### 3.4 §4B / AC-5 — what gates each shape

★★★★ **None of the three is gated by anything today, and that is the honest answer.**

- **Shape 2** moves code to a different address. ★★★ **The existing gates would catch a mistake**:
  `p3b` runs the probe end-to-end, and code poked to the wrong address does not execute — which is
  exactly how P6.64 §3.5's four-run bug surfaced. ★★ **That is coverage by consequence, not a row.**
- **Shape 3** changes code. Same gates, same argument.
- ★★★★★ **What neither has is a row that says "region B's layout is what the map claims"** — the
  assertion class that `memmap.inc` uses everywhere else. ★★★ **A `P3_REGIONB_*` span assertion is
  one `ifgt` and would be the cheapest thing in this whole arc.**

★★ **§4C(2): a seed-stack depth gate.** The cel arm now runs a 128-entry stack [P6.64 §3.2] and
nothing measures its depth. **Cost: a high-water byte in `ff_push` and one host read** — the same
shape as `p3_restbytes`. ★ Not built.

#### 3.5 ★★★★★ AC-6 — `$44` was never lost, and it is not a suite row

P6.64 said it was *"behind a diagnostic whose flag has not been found."* ★★★ **The flag is
`SYM.txt_bgx`**, `p3b_run.lua:883`, and the block is T-P0-088's:

> *"§4A's ONE READ: IS THE SOURCE CONTAMINATED? p3_restore_box copies SHADOW -> VISIBLE. If box
> pixels reached the SHADOW plane, the restore is innocent ... `$44` is the discriminator and
> nothing else is: it is the box's line colour and the one value a picture cannot supply."*

★★★★★ **It requires a TEXT arm and a message box actually on screen** — it samples while the
watchdog has parked the guest with the box still up. **The cel arm has no `txt_bgx`, so the block
cannot run there, and a text arm with no box gives a zero-width rectangle.**

★★★★ **So it is a task-specific diagnostic from a closed task, and it has been carried in six
dispatch AC lists as though it were a general suite row.** ★★★ **Retire it from the suite list.**
The dispatch's own words apply: *"a check nobody can run is not a check, and carrying it in six
reports is worse than retiring it."* ★★ It stays in the file, where it belongs, for the next
box-contamination question.

### 4 — Verification (AC-by-AC)

- **AC-1 [measurement] — MET.** §3.1, the full slot table quoted.
- **AC-2 [measurement] — MET.** §3.3.
- **AC-3 [measurement] — MET.** §3.1: forced by exhaustion, with the size/place distinction kept
  separate.
- **AC-4 [measurement] — MET.** §3.2: 4,166 B, verified in three arms.
- **AC-5 [measurement] — MET.** §3.4, including the answer "nothing, and here is what would".
- **AC-6 [measurement] — MET.** §3.5: flag found; recommended retired from the suite list.
- **AC-7 [byte-comparable] — MET.** `git diff --stat -- src/` empty.
- **AC-8 [suite] — DISCHARGED BY §2T CITATION.** Nothing changed since P6.64, which ran `p3b`,
  `comp 124/124`, `cel 9,193/9,193`, `pic 45/45`, `res 1,264/1,264` green against this exact tree.
- **AC-9 [ruling requested] — MET.** §7A.
- **AC-10 — MET.** §10.

### 5 — Verdict-time evidence (v0.7 §11)

**25.1 fresh tool output:** ★★ **No build of a changed tree.** The maps below are of the existing
arms, built to measure them:

```
$ lwasm --map ... -DP3B_COMBINED -DP3B_IRQ -DP3B_ACCEPT_OVERRUN
   region B: P3_PARSER_BASE $E000  P3_PARSER_END $E366  P3_CLNBUF $E390
             P3_FONT $E3BA + P3_FONT_BYTES $0800 -> ends $EBBA
             next symbol $FC00 (MAP_COVERAGE)      => 4,166 B unclaimed
             P3_REGIONB_END $FEF0

  symbols in $EBBA-$FC00:   cel 0   text 0   combined 0

$ git -C coco_agi diff --stat -- src/
(empty)
$ git -C coco_agi status --short
?? coco_agi.code-workspace
```

**25.2 bundled-artifact grep:** N/A.

**25.3 operator-runtime-smoke:** ★★ **N/A — nothing was built.** Not "pending Jay".

### 6 — Reactive deviations and route accounting

★★★ **No §6 trigger fired.** In particular the second — *"a shape needs `memmap.inc` changed"* —
**did not**: shape 2 puts code inside slot 7's existing span and moves no boundary in that file, so
POP and Karateka are untouched. ★★ **Shape 1 WOULD have needed it, and shape 1 is closed anyway.**

★★★★ **Nothing was implemented** [§6's first trigger, respected].

**Route accounting.** ★★★ **I proposed nothing and built nothing.** §7A is a ranked recommendation,
explicitly not a decision.

★ **One method note:** §3.2's figure was taken across three arms rather than one, because P6.64
corrected me for reporting a single measurement as corroborated. ★★ **That is not two routes to the
same number — it is one route applied to three configurations**, and it is described as that.

### 7 — Uncertainty flags

1. ★★★★ **"No symbol is declared there" is evidence from the symbol table, not proof nothing writes
   there at runtime.** The host stages the font at `P3_FONT` and the vocabulary at `P3_VOCAB`
   (`$A000`, not region B), and nothing else targets `$E000-$FEFF` — ★★★ **but a runtime write
   through a computed address would not appear in any map.** A span assertion (§3.4) is what would
   close this, and it does not exist.
2. ★★★ **4,166 B is the contiguous run BELOW `MAP_COVERAGE`.** There are a further 240 B at
   `$FE00-$FEF0`, and the 512 B at `$FC00-$FE00` are reserved only under `-DP3B_COVERAGE` — ★★ so
   the total could be 4,918 in a build without coverage. **The conservative figure is used.**
3. ★★★ **The coverage arm did not build** in the three-arm check (its flag needs companions I did
   not supply), so `$EBBA-$FC00` is confirmed clear in three arms and not in that fourth.
4. ★★ **Shape 2's risk is stated, not measured.** No code has been placed in slot 7 by this project
   except the parser, which was `org`ed there at the probe's creation rather than moved into it.
5. ★★ **`$44`'s retirement is a recommendation**, not an action — it lives in dispatch AC lists,
   which are the Orchestrator's [§2D].
6. ★ **Gates cited under §2T**, not re-run.

### 8 — Follow-up candidates

1. ★★★★★ **Take §7A's ruling and move 513 B into slot 7.** ★★★ The mechanism is the four-run poke,
   already in use and already debugged, and the space is 8× what is needed.
2. ★★★★ **A `P3_REGIONB_*` span assertion** — §3.4 and §7.1. ★★★ **One `ifgt`, and region B is the
   only major region in this probe without one.** It is also what would make shape 2 safe rather
   than merely measured.
3. ★★★★ **Retire `$44` from the suite AC list** [§3.5] and note in `gates.manifest` what it actually
   is.
4. ★★★ **Gate `p3b`'s seed-stack depth** — §3.4, a high-water byte. **Owed since P6.64 took the
   128-entry stack.**
5. ★★★ **The key arbitration** [P6.63 §3.4], still written down and unbuilt.
6. ★★ **The straddle clamp** — ninth task.
7. ★ `MAP_PRI_BANDS` — **thirteenth task**, and it sits at `$E000` in region B, where shape 2 wants
   to work. ★★ **If slot 7 is about to be used, this is the moment to delete an address nothing
   uses.**

### 9 — User interaction during task

**None.** §7A is the interaction this task is for.

### 10 — Candidate(s) captured this task

- `seeds/AGI/live/2026-09-19-the-table-that-closes-one-option-opens-another.md`
- `seeds/AGI/live/2026-09-19-a-diagnostic-from-a-closed-task-is-not-a-suite-row.md`

---

## 7A — ★★★★★ RULING REQUESTED — 513 bytes, and there are 4,166 of them

**Recommendation: shape 2 — put the overflow into slot 7.**

**What I found.** The arena aperture cannot move: its size is forced by a 10,964-byte LOGIC and its
*place* is forced by all eight MMU slots being allocated. That closes the shape you might have
expected to be the answer. **But the same table shows slot 7 — `$E000-$FEFF`, "resident tables",
never remapped in either phase — and it has a 4,166-byte hole** between the font's end and the
coverage tables. Unclaimed in the cel, text and combined arms.

**Why it is the right kind of space, not just space:**

- **Never remapped**, in either phase — the property code needs, and the same reason `CP_CEL` went
  to `MAP_INPUT` last task.
- **It already holds code.** The parser is `org`ed at `$E000` and runs 954 bytes. This is not a new
  technique.
- **It already holds relocated data.** The font moved there at P6.36 for exactly this reason.
- **The poke mechanism exists**: the image is already four runs, keyed on symbols. A fifth is free.
- **513 of 4,166 leaves 3,653** — which, unlike a shrink, is headroom that survives the next feature.

**What you would be giving up:** nothing measured. It touches no boundary in `memmap.inc`, so POP and
Karateka are unaffected.

**Two things I want on the record before you rule:**

1. ★★★ **"No symbol is declared there" is not "nothing writes there."** I checked three arms'
   symbol tables; a runtime write through a computed address would not show up. **Region B is the
   only major region in this probe with no span assertion**, and I would land that in the same task
   rather than after it.
2. ★★ **Shape 3 — shrinking 513 bytes — is not wrong, it is temporary.** This arc has re-consumed
   region A in seven of the last ten tasks, and P6.63's own finding was that freed space gets spent
   by the work that frees it.

**And one small thing to stop carrying:** `$44` is not a suite row. It is T-P0-088's message-box
contamination diagnostic, gated on a text arm with a box on screen. It has been in six AC lists as
an unrun suite check; it should come out of them and stay in the file where it belongs.

### 11 — Commit

`<this report>` — pushed to origin/wip before reporting. **No source commit: nothing was changed.**
